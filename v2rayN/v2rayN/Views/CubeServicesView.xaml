<UserControl
    x:Class="v2rayN.Views.CubeServicesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:ServiceLib.Resx;assembly=ServiceLib"
    d:DesignHeight="500"
    d:DesignWidth="560"
    Style="{StaticResource ViewGlobal}"
    mc:Ignorable="d">
    <Grid Width="520" Height="480" Margin="{StaticResource Margin8}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="{StaticResource Margin4}">
            <Button
                x:Name="btnClose"
                Width="24"
                Height="24"
                Command="materialDesign:DialogHost.CloseDialogCommand"
                DockPanel.Dock="Right"
                Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}">
                <materialDesign:PackIcon Kind="Close" />
            </Button>
            <Button
                x:Name="btnRefresh"
                Width="24"
                Height="24"
                DockPanel.Dock="Right"
                Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                ToolTip="{x:Static resx:ResUI.CubeServicesRefresh}">
                <materialDesign:PackIcon Kind="Refresh" />
            </Button>
            <TextBlock
                VerticalAlignment="Center"
                FontSize="16"
                Text="{x:Static resx:ResUI.CubeServicesTitle}" />
        </DockPanel>

        <Grid Grid.Row="1">
            <ProgressBar
                x:Name="progressBusy"
                Height="4"
                VerticalAlignment="Top"
                IsIndeterminate="True"
                Visibility="Collapsed" />
            <TextBlock
                x:Name="txtEmpty"
                Margin="{StaticResource Margin8}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="{x:Static resx:ResUI.CubeServicesEmpty}"
                TextWrapping="Wrap"
                Visibility="Collapsed" />
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="panelServices" Margin="{StaticResource Margin4}" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
